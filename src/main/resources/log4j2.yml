Configuration:
  status: error
  Appends:
    Console:
      name: STDOUT
      target: SYSTEM_OUT
      PatternLayout:
        pattern: "%d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n"
  Loggers:
    Root:
      level: debug
      AppenderRef:
        ref: STDOUT
